import { FliterByCategoryPipe } from './fliter-by-category.pipe';

describe('FliterByCategoryPipe', () => {
    it('create an instance', () => {
        const pipe = new FliterByCategoryPipe();
        expect(pipe).toBeTruthy();
    });
});
